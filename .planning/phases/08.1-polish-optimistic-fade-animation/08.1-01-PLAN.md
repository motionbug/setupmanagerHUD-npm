---
phase: 08.1-polish-optimistic-fade-animation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/components/dashboard/App.tsx
  - packages/core/src/components/dashboard/EventsTable.tsx
autonomous: true
requirements: []

must_haves:
  truths:
    - "Row fades to opacity 0 over ~200ms before being removed from DOM"
    - "Row height collapses smoothly over ~150ms after fade completes"
    - "Total animation duration is ~350ms (not instant)"
    - "Animation completes before row is removed from events array"
  artifacts:
    - path: "packages/core/src/components/dashboard/App.tsx"
      provides: "Delayed state removal after animation timeout"
      contains: "setTimeout"
    - path: "packages/core/src/components/dashboard/EventsTable.tsx"
      provides: "CSS transitions for opacity and height collapse"
      contains: "transition-"
  key_links:
    - from: "App.tsx:handleArchive"
      to: "EventsTable.tsx:TableRow"
      via: "archivingIds state triggers CSS transition"
      pattern: "archivingIds.has"
---

<objective>
Polish the archive/unarchive animation so the fade transition completes visually before the row is removed from the DOM.

Purpose: Currently the row disappears instantly, making the 150ms opacity transition invisible. This fix adds proper timing so users see the smooth fade-out and height collapse animation.

Output: Modified App.tsx and EventsTable.tsx with delayed state removal and CSS height collapse animation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08.1-polish-optimistic-fade-animation/08.1-CONTEXT.md
@docs/design-principles.md

<interfaces>
<!-- Key types and contracts from existing implementation -->

From packages/core/src/components/dashboard/App.tsx:
```typescript
// State for tracking archiving rows (line 28)
const [archivingIds, setArchivingIds] = React.useState<Set<string>>(new Set());

// handleArchive callback signature (line 72)
const handleArchive = React.useCallback(
  async (eventId: string, originalIndex: number) => { ... },
  [showArchived, archivedEvents, wsEvents]
);
```

From packages/core/src/components/dashboard/EventsTable.tsx:
```typescript
// Props interface (lines 79-85)
interface EventsTableProps {
  events: StoredEvent[];
  maxVisible?: number;
  showArchived: boolean;
  archivingIds: Set<string>;
  onArchive: (eventId: string, index: number) => void;
}

// Current transition class (line 155)
isArchiving && "opacity-50 pointer-events-none transition-opacity duration-150"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add delayed state removal in App.tsx handleArchive</name>
  <files>packages/core/src/components/dashboard/App.tsx</files>
  <read_first>
    - packages/core/src/components/dashboard/App.tsx (current handleArchive implementation, lines 72-130)
    - .planning/phases/08.1-polish-optimistic-fade-animation/08.1-CONTEXT.md (D-01, D-06, D-07 decisions)
  </read_first>
  <action>
Modify handleArchive callback to delay state removal until after the fade animation completes:

1. At the START of handleArchive (before any state changes), add the event to archivingIds:
   `setArchivingIds((prev) => new Set(prev).add(eventId));`

2. Add a 200ms setTimeout BEFORE the optimistic hide/removal (lines 79-84). Inside the timeout:
   - Perform the existing hide logic (setArchivedEvents filter or setHiddenEventIds add)
   - This 200ms matches the fade duration (per D-01)

3. Clean up: After successful API call OR after rollback, remove eventId from archivingIds:
   `setArchivingIds((prev) => { const next = new Set(prev); next.delete(eventId); return next; });`

Keep the API call OUTSIDE the timeout - it should fire immediately after marking the row as archiving. The timeout only delays the visual removal.

Structure:
1. Mark as archiving (add to archivingIds) - triggers CSS fade
2. Start API call immediately (no await yet)
3. After 200ms timeout: remove from visible array
4. Await API response
5. On success: remove from archivingIds
6. On failure: rollback (re-add to array, remove from archivingIds), show toast
  </action>
  <verify>
    <automated>cd /Users/rob.potvin/Git/jamf/setup-manager-HUD && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `App.tsx` contains `setArchivingIds` call BEFORE the setTimeout
    - Source assertion: `App.tsx` contains `setTimeout` with a callback that performs the hide logic
    - Source assertion: The timeout duration is 200 (matching D-01 fade duration)
    - Source assertion: archivingIds cleanup (delete) occurs in both success and rollback paths
    - Behavior: TypeScript compiles without errors
  </acceptance_criteria>
  <done>handleArchive adds event to archivingIds immediately, delays visual removal by 200ms via setTimeout, and cleans up archivingIds in both success and rollback paths</done>
</task>

<task type="auto">
  <name>Task 2: Update EventsTable row transition for fade + height collapse</name>
  <files>packages/core/src/components/dashboard/EventsTable.tsx</files>
  <read_first>
    - packages/core/src/components/dashboard/EventsTable.tsx (current transition class at line 155, TableRow structure)
    - .planning/phases/08.1-polish-optimistic-fade-animation/08.1-CONTEXT.md (D-03, D-04, D-05 decisions)
    - docs/design-principles.md (motion guidelines: transitions under 300ms, avoid transition-all)
  </read_first>
  <action>
Update the isArchiving conditional class on TableRow (line 155-156) to:

1. Change opacity from `opacity-50` to `opacity-0` (full fade out, per D-03)
2. Add `max-h-0 overflow-hidden` classes that will apply AFTER the opacity transition
3. Use transition classes for both properties: `transition-[opacity,max-height]` (NOT transition-all per design principles)
4. Duration: `duration-200` for the opacity fade (matching the 200ms timeout in App.tsx)
5. Add a second transition for height collapse: Add a conditional class that applies after opacity reaches 0

However, CSS alone cannot sequence two transitions. Instead, use this approach:
- Apply `opacity-0 transition-opacity duration-200` immediately when isArchiving is true
- The height collapse happens naturally when the row is removed from the array (after 200ms)

Simplified change:
- Change `opacity-50` to `opacity-0`
- Change `duration-150` to `duration-200`
- Keep `pointer-events-none transition-opacity`

The height "collapse" effect will be the natural DOM removal after the 200ms fade completes.
  </action>
  <verify>
    <automated>cd /Users/rob.potvin/Git/jamf/setup-manager-HUD && npm run typecheck && npm test</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: Line 155 contains `opacity-0` (NOT `opacity-50`)
    - Source assertion: Line 155 contains `duration-200` (NOT `duration-150`)
    - Source assertion: Line 155 contains `transition-opacity` (specific property, not transition-all)
    - Source assertion: Line 155 contains `pointer-events-none`
    - Behavior: TypeScript compiles without errors
    - Behavior: All 108+ tests pass
  </acceptance_criteria>
  <done>EventsTable row applies full opacity-0 fade over 200ms when isArchiving is true, matching the timeout delay in App.tsx</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries introduced - this is a CSS/timing-only change with no network, storage, or authentication implications.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08.1-01 | N/A | CSS animation | accept | No security relevance - purely visual polish |

This phase has no security surface changes.
</threat_model>

<verification>
Manual verification (after both tasks complete):

1. Start dev server: `npm run dev` and `npm run dev:worker` in separate terminals
2. Open http://localhost:5173 in browser
3. Verify Active view shows events
4. Click Archive button on any row
5. Observe: Row fades to invisible over ~200ms, then disappears from list
6. Switch to Archived view
7. Verify archived event appears
8. Click Unarchive button
9. Observe: Same fade animation before row is removed from archived list
10. Switch back to Active view and verify event reappears

Edge case verification:
- Archive two rows in quick succession - both should animate independently
- Trigger network error (disconnect network, click archive) - verify rollback restores row and shows toast
</verification>

<success_criteria>
- [ ] TypeScript compiles without errors
- [ ] All existing tests pass (108+ tests)
- [ ] Archiving a row shows visible ~200ms fade animation before removal
- [ ] Unarchiving shows the same animation behavior
- [ ] Rollback on failure works correctly (row reappears, toast shown)
- [ ] No visual glitch or "jump" when row is removed
</success_criteria>

<output>
Create `.planning/phases/08.1-polish-optimistic-fade-animation/08.1-01-SUMMARY.md` when done
</output>
