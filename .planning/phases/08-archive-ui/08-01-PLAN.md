---
phase: 08-archive-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/components/ui/tabs.tsx
  - packages/core/src/components/ui/sonner.tsx
  - packages/core/src/main.tsx
  - packages/core/src/components/dashboard/Filters.tsx
  - packages/core/src/components/dashboard/App.tsx
autonomous: true
requirements: [UI-01]

must_haves:
  truths:
    - "Admin can toggle between Active and Archived views via segmented control"
    - "Active view is the default state on page load"
    - "Switching to Archived view fetches archived records from API"
    - "Event count displays correct count for current view"
  artifacts:
    - path: "packages/core/src/components/ui/tabs.tsx"
      provides: "Radix Tabs primitives for segmented control"
      exports: ["Tabs", "TabsList", "TabsTrigger", "TabsContent"]
    - path: "packages/core/src/components/ui/sonner.tsx"
      provides: "Sonner toast component"
      exports: ["Toaster"]
    - path: "packages/core/src/main.tsx"
      provides: "App root with TooltipProvider and Toaster"
      contains: "TooltipProvider"
    - path: "packages/core/src/components/dashboard/Filters.tsx"
      provides: "Archive toggle segmented control"
      contains: "TabsTrigger"
    - path: "packages/core/src/components/dashboard/App.tsx"
      provides: "showArchived state and API query param wiring"
      contains: "showArchived"
  key_links:
    - from: "packages/core/src/components/dashboard/Filters.tsx"
      to: "packages/core/src/components/dashboard/App.tsx"
      via: "showArchived prop and onShowArchivedChange callback"
      pattern: "onShowArchivedChange"
    - from: "packages/core/src/components/dashboard/App.tsx"
      to: "/api/events"
      via: "fetch with archived query param"
      pattern: "archived=true"
---

<objective>
Add Active/Archived toggle to the dashboard filters.

Purpose: Admins need to switch between viewing active enrollments and archived records. This toggle changes the API query to include or exclude archived records.

Output: Segmented control in Filters bar, wired to App state, fetching correct data based on selection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-archive-ui/08-CONTEXT.md
@.planning/phases/08-archive-ui/08-RESEARCH.md
@.planning/phases/08-archive-ui/08-PATTERNS.md
@.planning/phases/08-archive-ui/08-UI-SPEC.md

<interfaces>
<!-- Phase 7 API endpoints (from 07-02-SUMMARY.md) -->

GET /api/events - returns non-archived records by default
GET /api/events?archived=true - returns all records including archived
PATCH /api/events/:id/archive - toggles archive state, returns { eventId, isArchived }

<!-- Existing Filters props (from Filters.tsx) -->
interface FiltersProps {
  filters: FilterState;
  onFiltersChange: (filters: FilterState) => void;
  events: StoredEvent[];
}

<!-- Existing App state (from App.tsx) -->
const [filters, setFilters] = React.useState<FilterState>({...});

<!-- Button variants (from button.tsx) -->
variant: "default" | "destructive" | "outline" | "secondary" | "ghost" | "link"
size: "default" | "sm" | "lg" | "icon"

<!-- Styling tokens (from Filters.tsx) -->
bg-control border-edge-subtle rounded-lg h-10 text-base
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn Tabs and Sonner components</name>
  <files>packages/core/src/components/ui/tabs.tsx, packages/core/src/components/ui/sonner.tsx</files>
  <read_first>
    - packages/core/src/components/ui/select.tsx (analog for shadcn component structure)
    - packages/core/src/components/ui/tooltip.tsx (analog for Radix wrapper pattern)
    - packages/core/package.json (verify dependencies)
  </read_first>
  <action>
Run shadcn CLI to install Tabs and Sonner components:
```
npx shadcn@latest add tabs sonner
```

If CLI fails or prompts interactively, create files manually:

1. Create tabs.tsx with Radix TabsPrimitive.Root, TabsPrimitive.List, TabsPrimitive.Trigger, TabsPrimitive.Content. Use cn() from @/lib/utils. Export Tabs, TabsList, TabsTrigger, TabsContent.

2. Create sonner.tsx importing Toaster from "sonner". Remove next-themes dependency (this project does not use it). Export Toaster component with classNames for toast styling matching shadcn theme variables: bg-background, text-foreground, border-border.

After creation, verify both @radix-ui/react-tabs and sonner packages are in package.json dependencies. If missing, run:
```
npm install @radix-ui/react-tabs sonner
```
  </action>
  <verify>
    <automated>cd /Users/rob.potvin/Git/jamf/setup-manager-HUD && npm run typecheck 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - packages/core/src/components/ui/tabs.tsx exports Tabs, TabsList, TabsTrigger, TabsContent
    - packages/core/src/components/ui/sonner.tsx exports Toaster
    - npm run typecheck passes with no errors related to tabs or sonner
    - package.json contains @radix-ui/react-tabs and sonner in dependencies
  </acceptance_criteria>
  <done>Both UI primitives available for import, typecheck passes</done>
</task>

<task type="auto">
  <name>Task 2: Add TooltipProvider and Toaster to main.tsx</name>
  <files>packages/core/src/main.tsx</files>
  <read_first>
    - packages/core/src/main.tsx (current entry point structure)
    - packages/core/src/components/ui/tooltip.tsx (TooltipProvider export)
    - packages/core/src/components/ui/sonner.tsx (Toaster export from Task 1)
    - .planning/phases/08-archive-ui/08-RESEARCH.md (Pitfall 1: Missing TooltipProvider, Pitfall 5: Sonner Toaster Not Mounted)
  </read_first>
  <action>
Modify main.tsx to wrap App with TooltipProvider and add Toaster:

1. Add imports:
   - TooltipProvider from @/components/ui/tooltip
   - Toaster from @/components/ui/sonner

2. Update render call:
   - Wrap App in TooltipProvider
   - Add Toaster as sibling after App (inside TooltipProvider)

Structure:
```
<TooltipProvider>
  <App />
  <Toaster />
</TooltipProvider>
```

TooltipProvider is required for Radix Tooltip to work (archive button tooltips). Toaster is required for Sonner toast.error() calls to display.
  </action>
  <verify>
    <automated>cd /Users/rob.potvin/Git/jamf/setup-manager-HUD && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - main.tsx imports TooltipProvider from @/components/ui/tooltip
    - main.tsx imports Toaster from @/components/ui/sonner
    - App is wrapped in TooltipProvider
    - Toaster appears after App inside TooltipProvider
    - npm run typecheck passes
  </acceptance_criteria>
  <done>Providers in place for tooltips and toasts to function</done>
</task>

<task type="auto">
  <name>Task 3: Add showArchived state and API wiring to App.tsx</name>
  <files>packages/core/src/components/dashboard/App.tsx</files>
  <read_first>
    - packages/core/src/components/dashboard/App.tsx (current state management)
    - packages/core/src/hooks/useWebSocket.ts (how events are currently fetched)
    - .planning/phases/08-archive-ui/08-PATTERNS.md (App.tsx pattern section)
    - .planning/phases/07-database-api-foundation/07-02-SUMMARY.md (API endpoint details)
  </read_first>
  <action>
Add archive state management to App.tsx:

1. Add new state:
   - const [showArchived, setShowArchived] = React.useState(false);

2. Create fetchEvents effect that re-fetches when showArchived changes:
   - Fetch from /api/events (active) or /api/events?archived=true (archived)
   - This supplements WebSocket events with server-side filtered data
   - Store fetched events in a separate state or merge with WebSocket events

3. Filter WebSocket events client-side based on showArchived:
   - When showArchived is false, exclude events where is_archived is true (though new WebSocket events are never archived)
   - Note: Per RESEARCH.md, WebSocket events are always new (never archived on creation), so filtering is minimal

4. Update Filters component usage to pass new props:
   - showArchived={showArchived}
   - onShowArchivedChange={setShowArchived}

5. When showArchived changes, trigger a re-fetch of events with appropriate query param.

Note: The current useWebSocket hook fetches history via WebSocket message. For archive filtering, we need an HTTP fetch to /api/events with query param since WebSocket history does not support filtering.
  </action>
  <verify>
    <automated>cd /Users/rob.potvin/Git/jamf/setup-manager-HUD && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - App.tsx has showArchived state initialized to false
    - App.tsx has setShowArchived function
    - App.tsx passes showArchived and onShowArchivedChange to Filters
    - When showArchived is true, fetch calls /api/events?archived=true
    - npm run typecheck passes
  </acceptance_criteria>
  <done>App manages archive view state and fetches correct data</done>
</task>

<task type="auto">
  <name>Task 4: Add Active/Archived toggle to Filters.tsx</name>
  <files>packages/core/src/components/dashboard/Filters.tsx</files>
  <read_first>
    - packages/core/src/components/dashboard/Filters.tsx (current filter UI)
    - packages/core/src/components/ui/tabs.tsx (Tabs component from Task 1)
    - .planning/phases/08-archive-ui/08-UI-SPEC.md (Interaction Contracts: Archive Toggle)
    - .planning/phases/08-archive-ui/08-PATTERNS.md (Filters.tsx pattern section)
    - .planning/phases/08-archive-ui/08-CONTEXT.md (Decision: Archive Toggle Placement)
  </read_first>
  <action>
Add segmented control to Filters using Tabs component:

1. Add imports:
   - Tabs, TabsList, TabsTrigger from @/components/ui/tabs

2. Extend FiltersProps interface:
   - showArchived: boolean
   - onShowArchivedChange: (showArchived: boolean) => void

3. Add Tabs component after Model filter, before Clear filters:
   - Position: After existing Select filters, before the flex-1 spacer
   - Use Tabs with defaultValue="active"
   - onValueChange converts value to boolean: (v) => onShowArchivedChange(v === "archived")

4. Style TabsList and TabsTrigger per UI-SPEC.md:
   - TabsList: className="bg-control border border-edge-subtle rounded-lg p-1"
   - TabsTrigger: className="h-8 px-4 text-sm font-medium rounded-md data-[state=active]:bg-surface data-[state=active]:shadow-sm"
   - Active trigger: text-ink
   - Inactive trigger: text-ink-muted hover:text-ink

5. Use controlled value prop based on showArchived:
   - value={showArchived ? "archived" : "active"}

6. Optional: When showArchived is true, display "Viewing archived" badge near event count using status-badge-neutral styling.
  </action>
  <verify>
    <automated>cd /Users/rob.potvin/Git/jamf/setup-manager-HUD && npm run typecheck 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - Filters.tsx imports Tabs, TabsList, TabsTrigger
    - FiltersProps includes showArchived: boolean and onShowArchivedChange
    - Tabs component renders with "Active" and "Archived" triggers
    - TabsList has className containing bg-control border-edge-subtle rounded-lg
    - TabsTrigger has data-[state=active] styling
    - npm run typecheck passes
  </acceptance_criteria>
  <done>Segmented control visible in Filters bar, wired to parent state</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client->API | Fetch requests cross from browser to Worker API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | archived query param | accept | Archived records are not sensitive; same auth protects both views |
| T-08-02 | Tampering | showArchived state | accept | Client-side state; server validates all API calls independently |
</threat_model>

<verification>
1. Run typecheck: `npm run typecheck`
2. Run dev server: `npm run dev:worker`
3. Open dashboard at localhost:8787
4. Verify Tabs segmented control appears after Model filter
5. Click "Archived" tab - dashboard should show archived records (may be empty)
6. Click "Active" tab - dashboard should show non-archived records
7. Verify event count updates when switching tabs
</verification>

<success_criteria>
- Tabs component installed and renders without errors
- Toaster component installed and mounted in app root
- TooltipProvider wraps the entire app
- Active/Archived toggle visible in Filters bar
- Switching tabs changes which events are displayed
- Default view is "Active"
- No TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/08-archive-ui/08-01-SUMMARY.md` when done
</output>
